import React, { useState } from "react";

const App = () => {
  const [count, setCount] = useState(0);

  let status = true;

  return (
    <div>
      <h1>Login status</h1>

      {}
    </div>
  );
};

export default App;
